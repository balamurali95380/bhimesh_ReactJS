const initialState=["ravi","rani"]
export const UserReducer=(state=initialState,action)=>{
    switch (action.type) {
        case "Create":
            
            break;
    
            default:
                return state
    }

}