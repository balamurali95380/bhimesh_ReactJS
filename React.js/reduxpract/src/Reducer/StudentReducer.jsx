const initialState=["ravi","rani"]
export const studentReducer=(state=initialState,action)=>{
    switch (action.type) {
        case "Create":
            
            break;
    
            default:
                return state
              
    }

}