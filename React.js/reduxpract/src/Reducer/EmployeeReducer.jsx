const initialState=["ravi","rani"]
export const EmployeeReducer=(state=initialState,action)=>{
    switch (action.type) {
        case "Create":
            
            break;
    
        default:
            return state
            
    }

}